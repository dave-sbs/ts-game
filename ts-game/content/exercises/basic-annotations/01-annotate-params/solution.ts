// Solution: Annotate Function Parameters

function greet(name: string, age: number) {
  return `Hello ${name}, you are ${age} years old!`;
}

// Test calls (don't modify these)
const message1 = greet("Alice", 25);
const message2 = greet("Bob", 30);

export { greet };
