// Type tests for the exercise
import { username, userAge, isVerified, accountBalance, welcomeMessage } from './exercise';

// Verify variable types
const _name: string = username;
const _age: number = userAge;
const _verified: boolean = isVerified;
const _balance: number = accountBalance;
const _message: string = welcomeMessage;
